<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!--<item name="android:fontFamily">@font/lato</item>-->
        <!--<item name="fontFamily">@font/lato</item>-->

        <item name="android:textViewStyle">@style/LatoTextViewStyle</item>
        <item name="android:buttonStyle">@style/LatoButtonStyle</item>
        <item name="android:radioButtonStyle">@style/LatoRadioButtonStyle</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <style name="ToolbarStyle" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">@color/white</item>
    </style>

    <!-- Rating bar -->
    <style name="RatingBar" parent="@style/Widget.AppCompat.RatingBar.Small">

    </style>




    <style name="LatoTextViewStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/lato_regular</item>
    </style>

    <style name="LatoButtonStyle" parent="android:Widget.Holo.Button">
        <item name="android:fontFamily">@font/lato_regular</item>
    </style>

    <style name="LatoRadioButtonStyle" parent="android:Widget.CompoundButton.RadioButton">
        <item name="android:fontFamily">@font/lato_regular</item>
    </style>


</resources>
